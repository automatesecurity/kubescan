apiVersion: apps/v1
kind: Deployment
metadata:
  name: multi
  namespace: payments
spec:
  template:
    spec:
      containers:
        - name: api
          image: ghcr.io/acme/api:1.0.0
        - name: worker
          image: ghcr.io/acme/worker:2.0.0
